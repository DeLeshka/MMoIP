#pragma once
#include <string>
#include <fstream>
#include <vector>

void input_number(double& subject, const bool& int_check = 0);
void input_and_check(std::string& subject, const std::string& name);
void show_menu();
void menu_update();